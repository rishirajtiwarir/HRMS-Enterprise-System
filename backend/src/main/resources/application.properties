# ===================================================================
# SERVER CONFIGURATION
# ===================================================================
server.port=8080
server.servlet.context-path=/api/v1

# ===================================================================
# DATABASE CONFIGURATION (MySQL)
# ===================================================================
# Change 'localhost:3306/hrms_db' to match your MySQL environment settings
spring.datasource.url=jdbc:mysql://byvyaipwlyy56fovhtln-mysql.services.clever-cloud.com:3306/byvyaipwlyy56fovhtln?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=uxxvw7ivl4mvhdir
spring.datasource.password=XfLqYMCCyGyD8uIZhuMk
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===================================================================
# JPA / HIBERNATE CONFIGURATION
# ===================================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# Enables execution of schema.sql on startup if needed (or we can use standard Hibernate ddl auto)
spring.sql.init.mode=never

# ===================================================================
# SECURITY & JWT CONFIGURATION
# ===================================================================
# Base64 encoded secret key for signing JWTs (must be at least 256-bit secure)
# The default secret below represents: "enterpriseLevelHrmsJwtSecretKeyForDemoFinalYearProjectSecurityCheck2026" in hexadecimal
hrms.jwt.secret=656e74657270726973654c6576656c48726d734a77745365637265744b6579466f7244656d6f46696e616c5965617250726f6a6563745365637572697479436865636b32303236
# Access token expiration: 15 minutes (in milliseconds)
hrms.jwt.expiration-ms=900000
# Refresh token expiration: 7 days (in milliseconds)
hrms.jwt.refresh-expiration-ms=604800000

# ===================================================================
# MULTIPART FILE UPLOAD (For Employee Profile Images)
# ===================================================================
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
# Custom upload directory
app.upload.dir=./uploads

# ===================================================================
# SWAGGER / OPENAPI CONFIGURATION
# ===================================================================
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operations-sorter=alpha
